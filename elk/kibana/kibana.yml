server.name: kibana
server.host: "0.0.0.0"
server.port: 5601

elasticsearch.hosts: ["http://elasticsearch:9200"]

# Interface en français (optionnel)
i18n.locale: "fr"

# Monitoring
monitoring.ui.container.elasticsearch.enabled: true
